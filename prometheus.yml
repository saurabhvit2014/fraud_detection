# Prometheus Configuration for Phase 3 Monitoring

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'phase3-monitoring'

# Alert configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets: []

# Load rules
rule_files:
  - "/etc/prometheus/rules/*.yml"

# Scrape configurations
scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # FastAPI metrics
  - job_name: 'transaction-risk-api'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['localhost:8000']
    scrape_interval: 10s
    scrape_timeout: 5s
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance

  # Node exporter (if available)
  - job_name: 'node'
    static_configs:
      - targets: ['localhost:9100']
    scrape_interval: 15s

  # Kubernetes API server (for K8s deployments)
  - job_name: 'kubernetes-apiservers'
    kubernetes_sd_configs:
      - role: endpoints
    scheme: https
    tls_config:
      ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
    bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
    relabel_configs:
      - source_labels: [__meta_kubernetes_namespace, __meta_kubernetes_service_name, __meta_kubernetes_endpoint_port_name]
        action: keep
        regex: default;kubernetes;https

# Alert rules (additional file)
# Create /etc/prometheus/rules/phase3_alerts.yml with:
#
# groups:
#   - name: api_alerts
#     interval: 30s
#     rules:
#       - alert: HighErrorRate
#         expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.05
#         for: 5m
#         labels:
#           severity: critical
#         annotations:
#           summary: "High error rate detected"
#
#       - alert: APIDown
#         expr: up{job="transaction-risk-api"} == 0
#         for: 1m
#         labels:
#           severity: critical
#         annotations:
#           summary: "Transaction Risk API is down"
#
#       - alert: HighLatency
#         expr: histogram_quantile(0.95, http_request_duration_seconds) > 1
#         for: 5m
#         labels:
#           severity: warning
#         annotations:
#           summary: "High API latency detected"
